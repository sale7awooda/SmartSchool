-- Initial schema — Smart School Management System
--
-- This migration was NOT generated via pg_dump. It represents the cumulative
-- state of all previous ad-hoc migrations that were applied manually through
-- the Supabase SQL Editor.
--
-- To regenerate a fresh dump of the remote schema when Docker is available:
--   supabase db dump --linked --schema public --file supabase/migrations/new_dump.sql
--
-- For future schema changes, create new migration files named:
--   <timestamp>_<description>.sql
-- Then apply them via:
--   supabase db query --linked --file supabase/migrations/<filename>
